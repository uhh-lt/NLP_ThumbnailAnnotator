sudo: required

services:
  - docker

language: java

jdk:
  - oraclejdk8

script:
  - docker --version
  - docker-compose --version
  - cd thumbnailAnnotator.parent
  - mvn clean verify
  - mvn package
  - mvn install -Dmaven.javadoc.skip=true -B -V
